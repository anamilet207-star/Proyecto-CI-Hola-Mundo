name: Notificacion CI

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar notificación a ntfy
        run: |
          curl -d "Nuevo push al repositorio 🚀" https://ntfy.sh/devops-itla